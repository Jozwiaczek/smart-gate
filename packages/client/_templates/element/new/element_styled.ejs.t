---
to: src/elements/<%= h.changeCase.pascal(Name) %>/<%= h.changeCase.pascal(Name) %>.styled.ts
---
import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
`;

export default Wrapper;
